---
plugin: aws_ec2
regions:
  - us-east-1
#filters:
#  tag:Name: production*
#  instance-state-name: running

keyed_groups:
 - key: tags
   prefix: tag
 - key: instance_type
    prefix: instance_type